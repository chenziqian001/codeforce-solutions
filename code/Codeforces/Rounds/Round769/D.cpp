#include<bits/stdc++.h>
using namespace std;




int main(){
    int n;
    cin>>n;
    vector<int> a(n);
    for(int i=0;i<n;i++) cin>>a[i];
    vector<int> res(n);
    vector<pair<int,int>> g;
    for(int i=0;i<n;i++){
        g.emplace_back(a[i],i);
        int j=0;
        
    }

}